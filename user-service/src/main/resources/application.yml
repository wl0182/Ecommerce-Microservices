spring:
  application:
    name: user-service

server:
  port:
    7001 # Change the port number to 8081


eureka:
    client:
        service-url:
          defaultZone: http://localhost:8761/eureka/

